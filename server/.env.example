MONGODB_URI=
JWT_SECRET=
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# If RESEND_API_KEY is set, it takes priority over SMTP above (needed on
# hosts that block outbound SMTP ports, e.g. Render's free tier).
RESEND_API_KEY=
RESEND_FROM=
